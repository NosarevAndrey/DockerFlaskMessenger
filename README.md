Щоб запустити контейнер, використовуючи docker-compose необхідно перейти в папку із файлом compose.yaml(root) та виконати команду docker-compose up -d
Щоб запустити bash скрипт необходно перейти в папку, де він міститься (FlaskServer) та виконати команду bash launch_container.sh -p [№Порта]
